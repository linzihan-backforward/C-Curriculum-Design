#pragma once
#include<Windows.h>
LRESULT CALLBACK WindowProc(//声明主窗口消息响应函数
	_In_ HWND hwnd,
	_In_ UINT uMsg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG1(//声明对话框一的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG2(//声明对话框二的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG3(//声明对话框三的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG4(//声明对话框四的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG5(//声明对话框五的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG6(//声明对话框六的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG7(//声明对话框七的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG8(//声明对话框八的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG9(//声明对话框九的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG10(//声明对话框十的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG11(//声明对话框十一的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG12(//声明对话框十二的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG13(//声明对话框十三的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG14(//声明对话框十四的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG15(//声明对话框十五的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG16(//声明对话框十六的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);
INT_PTR CALLBACK DIALOG17(//声明对话框十七的响应函数
	_In_ HWND hdlg,
	_In_ UINT msg,
	_In_ WPARAM wParam,
	_In_ LPARAM lParam
);